//----------------------------------------------------------------------------------------------------;

//	Mac 용;

//----------------------------------------------------------------------------------------------------;

var db0 = db.getSiblingDB( "_schema" );
var cols = db0.getCollectionNames();
var i=0; iLen=cols.length;
for( ; i<iLen; ++i )
{
	var col = db0.getCollection( cols[ i ] );
		col.remove({});
}

//----------------------------------------------------------------------------------------------------;